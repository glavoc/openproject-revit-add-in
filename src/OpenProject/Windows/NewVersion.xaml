<Window
  x:Class="OpenProject.Windows.NewVersion"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Title="A new BCFier is available!"
  MinWidth="300"
  Icon="../Assets/openproject.ico"
  SizeToContent="WidthAndHeight">
  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="auto" />
      <RowDefinition Height="auto" />
      <RowDefinition Height="1*" />
    </Grid.RowDefinitions>

    <TextBlock Name="Description" Margin="5" />

    <StackPanel
      Grid.Row="2"
      Margin="5,15,5,5"
      HorizontalAlignment="Right"
      Orientation="Horizontal">
      <Button
        Width="75"
        Margin="5"
        Click="Button_OK"
        Content="Yes"
        IsDefault="True" />
      <Button
        Width="75"
        Margin="5"
        Background="Transparent"
        BorderBrush="Transparent"
        Click="Button_Cancel"
        Content="Maybe later" />
    </StackPanel>
  </Grid>
</Window>
